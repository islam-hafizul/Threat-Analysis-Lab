rules:
  - name: "multiple_failed_root"
    pattern: "Failed password for root"
    threshold: 3
    time_window: "5m"
    severity: "HIGH"
  
  - name: "off_hours_login"
    pattern: "Accepted password"
    condition: "time.hour < 6 or time.hour > 20"
    severity: "MEDIUM"